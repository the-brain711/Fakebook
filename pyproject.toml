[project]
name = "Fakebook"
version = "0.1.0"
description = "Facebook-like social media web application"
dependencies = [
    "flask",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"